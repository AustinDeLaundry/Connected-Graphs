This program basically prints out each of the connected vertices connected to a single vertex in a graph by multiplying matrices together.

Input file should look like the samples in the data folder. 